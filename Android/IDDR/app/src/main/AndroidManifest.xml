<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.project.bangkit.cap0475.iddr">

    <queries>
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
    </queries>

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.IDDR">
        <activity android:name="com.project.bangkit.cap0475.iddr.view.DetailActivity"></activity>
        <activity android:name="com.project.bangkit.cap0475.iddr.view.LoginActivity" />
        <activity
            android:name="com.project.bangkit.cap0475.iddr.view.DetectActivity"
            android:screenOrientation="portrait" />
        <activity android:name="com.project.bangkit.cap0475.iddr.view.MainActivity" />
        <activity
            android:name="com.project.bangkit.cap0475.iddr.view.SplashScreenActivity"
            android:theme="@style/Theme.IDDR.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.project.bangkit.cap0475.iddr.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>